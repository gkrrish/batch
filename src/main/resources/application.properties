spring.application.name=batch

server.port=8385

spring.datasource.url=jdbc:oracle:thin:@localhost:1521/xe
spring.datasource.driverClassName=oracle.jdbc.OracleDriver
spring.datasource.username=krishna
spring.datasource.password=New_2024

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs


spring.data.redis.host=localhost
spring.data.redis.port=6379

# Jedish is Synchronous
spring.data.redis.jedis.pool.enabled=true
spring.data.redis.jedis.pool.max-active=8
spring.data.redis.jedis.pool.max-wait=-1
spring.data.redis.jedis.pool.max-idle=4
spring.data.redis.jedis.pool.min-idle=0

#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true


#SpringBatch needs a predefine batch schema. we can create manually or if we use below propertly
#observe: is it causes the existing database tables?
spring.batch.jdbc.initialize-schema=always
#spring.batch.jdbc.table-prefix=BATCH_
spring.batch.jdbc.schema=classpath:schema/batch_schema.sql
#https://github.com/spring-projects/spring-batch/blob/5.1.x/spring-batch-core/src/main/resources/org/springframework/batch/core/schema-oracle.sql
